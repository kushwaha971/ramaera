import { Box, styled } from "@mui/material";

const FooterStyle = styled(Box)(({theme}) =>({
    backgroundImage: `linear-gradient(to  right,#1E1E1E 0%,#4b4b4b 100% )`,
   ".PolygonStyle":{
    display:"flex",
  justifyContent:"space-between",
   },

}));
export default FooterStyle;